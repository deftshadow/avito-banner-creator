export const textStyles = `z-index: 100;
    background-color: transparent;
    color: white;
    border: none;
    padding: 0;
    overflow: hidden;
    position: absolute;`;

export const bannerStyles = `position: relative;
    border-radius: 5px;
    box-sizing: content-box;
    display: inline-block;`;
